\section{Experiments and Results}


Detail exactly how you conducted the experiment so someone else could replicate it
\subsection{Setup}
\subsection{Dataset Generation}

To evaluate the performance of the TSP algorithms a synthetic benchmark dataset of Euclidean TSP instances is generated. 
The generation process is controlled by a Python script that creates two types of point distributions: \textit{uniform} and \textit{clustered}. 
The script uses a fixed random seed (\texttt{42}) to ensure reproducibility.

\subsubsection{Instance sizes and repetitions}
For each problem size \(n \in \{10, 25, 50, 100, 200, 500\}\) 30 independent instances were generated. 
The same set of instances is used for all algorithms, allowing a fair comparison.

\subsubsection{Uniform instances}
Uniform instances are created by drawing \(n\) coordinates independently from a uniform distribution over the unit square \([0,1]^2\). 
The Euclidean distance between every pair of coordinates is computed to form a symmetric distance matrix.

\subsubsection{Clustered instances}
Clustered instances are designed to mimic real-world scenarios where coordinates of e.g. cities or harbors form clusters.
The number of clusters is set to \(\max(1, n // 10)\). 
Cluster centers are sampled uniformly from \([0.1, 0.9]^2\) (avoiding the extreme borders). 
Each node is assigned to a center in a round-robin fashion
and its coordinates are drawn from a normal distribution around that center with a standard deviation of \(0.05\). 
The resulting coordinates are clipped to the unit square to keep all points inside \([0,1]^2\). 
The distance matrix is then computed as for the uniform case.

\subsubsection{Storage format}
Each instance is stored in two NumPy binary files (``.npy``):
\begin{itemize}
    \item \texttt{\{id\}\_nodes.npy}: an \(n \times 2\) array of node coordinates.
    \item \texttt{\{id\}\_edges.npy}: an \(n \times n\) matrix of pairwise Euclidean distances.
\end{itemize}
The directory structure is:
\begin{verbatim}
data/
├── uniform/
│   ├── 10/
│   │   ├── 0_nodes.npy
│   │   ├── 0_edges.npy
│   │   ├── 1_nodes.npy 
│   │   ├── ...
│   ├── 25/
│   └── ...
└── clustered/
    ├── 10/
    └── ...
\end{verbatim}

\subsubsection{Test data option}
The script also provides a \texttt{--testdata} flag that writes the generated instances into a separate \texttt{data/test/} directory. 
This can be used for hyperparameter tuning or preliminary experiments without interfering with the main benchmark dataset.

All dataset generation parameters (random seed, cluster spread, number of repetitions) are kept constant throughout the experiments to ensure a consistent basis for comparison.


\subsubsection{Benchmark}
Mention greedy baseline

\subsubsection{Hardware / Environment}

\subsection{Results}

\begin{itemize}
	\item Performance Metrics
	\item Plots
\end{itemize}

\begin{figure}[H]
	\begin{center}
		\includegraphics[width=0.95\linewidth]{figures/optimality_gap_lines_clustered.png}
	\end{center}
	\caption{Optimality gap of clustered algorithms for different sizes and distributions}\label{fig:optimality_gap_lines_clustered}
\end{figure}

\begin{figure}[H]
	\begin{center}
		\includegraphics[width=0.95\linewidth]{figures/optimality_gap_lines_uniform.png}
	\end{center}
	\caption{Optimality gap of uniformed algorithms for different sizes and distributions}\label{fig:optimality_gap_lines_uniform}
\end{figure}


% \begin{figure}[H]
% 	\begin{center}
% 		\includegraphics[width=0.95\linewidth]{figures/optimality_gap_lines.png}
% 	\end{center}
% 	\caption{Optimality gap of algorithms for different sizes and distributions}\label{fig:optimality_gap_lines}
% \end{figure}

% \textcolor{red}{Probably split Figure~\ref{fig:optimality_gap_lines} in half, so that it is nicely visible in single column layout.}

% \begin{figure}[H]
% 	\begin{center}
% 		\includegraphics[width=0.95\linewidth]{figures/time_scaling.png}
% 	\end{center}
% 	\caption{Time scaling of algorithms for different sizes and distributions.}\label{fig:time_scaling}
% \end{figure}

\begin{figure}[H]
	\begin{center}
		\includegraphics[width=0.95\linewidth]{figures/time_scaling_clustered.png}
	\end{center}
	\caption{Time scaling of clustered algorithms for different sizes and distributions.}\label{fig:time_scaling_clustered}
\end{figure}

\begin{figure}[H]
	\begin{center}
		\includegraphics[width=0.95\linewidth]{figures/time_scaling_uniform.png}
	\end{center}
	\caption{Time scaling of uniformerd algorithms for different sizes and distributions.}\label{fig:time_scaling_uniformed}
\end{figure}